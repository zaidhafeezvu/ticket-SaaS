# Database
DATABASE_URL="postgresql://user:password@localhost:5432/ticketsaas"

# Better Auth Configuration
# Generate a secure secret key with: openssl rand -base64 32
BETTER_AUTH_SECRET="your-super-secret-key-change-this-in-production"

# Server-side auth URL (used by the auth server)
BETTER_AUTH_URL="http://localhost:3000"

# Client-side auth URL (used by the browser)
# Optional: If not set, will use relative URLs (recommended for production)
# For local development, you can set it to match BETTER_AUTH_URL
NEXT_PUBLIC_BETTER_AUTH_URL="http://localhost:3000"

# GitHub OAuth Configuration
# Get these from: https://github.com/settings/developers
# Create a new OAuth App with:
#   - Homepage URL: http://localhost:3000 (or your domain)
#   - Authorization callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID="your-github-oauth-client-id"
GITHUB_CLIENT_SECRET="your-github-oauth-client-secret"

# Email Configuration (Resend)
# Get your API key from: https://resend.com/api-keys
# Required for email verification
RESEND_API_KEY="your-resend-api-key"
EMAIL_FROM="noreply@yourdomain.com"

# For production deployment:
# 1. Set BETTER_AUTH_URL to your production domain (e.g., "https://yourdomain.com")
# 2. Leave NEXT_PUBLIC_BETTER_AUTH_URL unset to use relative URLs (recommended)
#    OR set it to your production domain if you need absolute URLs
# 3. Update GitHub OAuth App callback URL to: https://yourdomain.com/api/auth/callback/github
